# frozen_string_literal: true

source "https://rubygems.org"

ruby ">= 3.2"

# Official Stripe Ruby SDK. Use Stripe::StripeClient (not the deprecated
# global Stripe.api_key assignment). Prefer a restricted key (rk_) in STRIPE_API_KEY.
gem "stripe", "~> 19.6"
