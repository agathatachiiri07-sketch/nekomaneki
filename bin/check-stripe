#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require_relative "../lib/nekomaneki/payments"

puts "stripe gem #{Stripe::VERSION}"
puts "StripeClient ready (#{Nekomaneki::Payments::API_VERSION})"
